# Simple/minimalized HTTP (GET/POST) requests library for ESP32, ESP8266 

- Just based on `WiFiClient.h` and `WiFiSecureClient.h` without String objects
- Simple, straigh API. Flexible, easy to extend